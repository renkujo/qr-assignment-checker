# Domain owner

This folder is reserved for focused domain helpers/types once the implementation reaches this area. Keep route wiring in `src/routes/` and move repeated domain logic here only when the code earns it.
